const FONTS = {
  NOTO_EMOJI: "Noto Emoji",
};

const FONT_STATE = {
  LOADED: "loaded",
  FALLBACK: "fallback",
};

const FONT_CONFIG = {
  FONTS,
  STATE: FONT_STATE,
};

export default FONT_CONFIG;
