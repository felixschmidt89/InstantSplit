{
  "name": "instant-split",
  "version": "1.0.1",
  "private": true,
  "description": "MERN monorepo root for InstantSplit application",
  "scripts": {
    "dev": "npx concurrently \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "start:client": "npm run dev --workspace=client",
    "start:server": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "workspaces": [
    "client",
    "server"
  ]
}
