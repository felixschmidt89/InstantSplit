const HEADERS = {
  GROUPCODE: "x-group-code",
  CONTENT_TYPE: "content-type",
};

const CONTENT_TYPES = {
  JSON: "application/json",
  MULTIPART: "multipart/form-data",
};

const API_HEADER_CONSTANTS = {
  HEADERS,
  CONTENT_TYPES,
};

export default API_HEADER_CONSTANTS;
