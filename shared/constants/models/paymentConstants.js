const FIELDS = {
  AMOUNT: "amount",
  MAKER: "maker",
  RECIPIENT: "recipient",
};

const TRANSACTION_TYPE_VALUE = "payment";

const PAYMENT = {
  FIELDS,
  TYPE_VALUE: TRANSACTION_TYPE_VALUE,
};

export default PAYMENT;
