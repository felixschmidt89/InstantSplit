const FIELDS = {
  DEBTOR: "debtor",
  CREDITOR: "creditor",
  AMOUNT: "amount",
};

const TRANSACTION_TYPE_VALUE = "settlement";

const SETTLEMENT = {
  FIELDS,
  TYPE_VALUE: TRANSACTION_TYPE_VALUE,
};

export default SETTLEMENT;
