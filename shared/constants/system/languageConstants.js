const GERMAN = "de";
const ENGLISH = "en";

const LANGUAGES = {
  GERMAN,
  ENGLISH,
};

const DEFAULT_LANGUAGE = GERMAN;

const LANGUAGE_LOCALES = {
  [GERMAN]: "de-DE",
  [ENGLISH]: "en-GB",
};

const LANGUAGE = {
  LANGUAGES,
  DEFAULT_LANGUAGE,
  LANGUAGE_LOCALES,
};

export default LANGUAGE;
